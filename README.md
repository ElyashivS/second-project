# second-project

To run the tests, open the PowerShell and type:
```
npx wdio run wdio.conf.js 
```

To run the Allure report, open CMD and type:
```
allure generate allure-results --clean && allure open
```